$peer = 100% / 24;
.loli-row
  &:after
    content ""
    display table
    clear both

.loli-col
  float left
  box-sizing border-box

for $grid in 1..24
  .loli-col-{$grid}
    width $grid * $peer
  .loli-col-offset-{$grid}
    margin-left $grid * $peer
